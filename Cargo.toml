[package]
name = "portuario"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
description = "Concurrency-safe free-port picking for test suites, guarded by OS file locks"
license = "MIT OR Apache-2.0"
keywords = ["port", "testing", "nextest", "tcp", "lock"]
categories = ["development-tools::testing", "network-programming"]

[dependencies]
socket2 = "0.6.5"

[dev-dependencies]
seq-macro = "0.3.6"
